spring:
  datasource:
    url: ${POSTGRES_HOST:jdbc:postgresql://localhost:5432/clickbus-places}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:senha1}
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    generate-ddl: true
